language: python
python:
  - "3.7.9"
  - "3.9"

install:
  - pip install -r tests/requirements.txt
  - pip install .

env:
  - PYTHONPATH=.

# command to run tests
script:
  - python -m unittest discover tests
